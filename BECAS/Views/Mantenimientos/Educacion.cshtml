@model BECAS.Models.VM.EducacionVM
@{
    ViewData["Title"] = "Educacion";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
<div class="card">
    <div class="card-header">
        <h3 class="card-title">Filtros</h3>
    </div>
    <div class="card-body">
        <div class="row">
            <form class="form-inline">
                <div class="form-group mb-2">
                    <select class="form-control select2" name="socios" id="socios" multiple="multiple" data-placeholder="-- Seleccionar Socio --" style="width:100%">
                        @foreach (var item in Model.Socios)
                        {
                            <option value="@item.IdImplementador">@item.Nombre</option>
                        }
                    </select>
                </div>
                <div class="form-group mb-2">
                    <select class="form-control select2" name="dropzona" id="dropzona" multiple="multiple" data-placeholder="-- Seleccionar Zona --" style="width:100%">
                        @*                        @foreach (var item in Model.LIstaSedes)
                            {
                            <option value="@item.IdSede">@item.Nombre</option>
                            }*@
                    </select>
                </div>

                <div class="form-group mb-2">
                    <select class="form-control select2" name="idprograma" id="idprograma" multiple="multiple" data-placeholder="-- Seleccionar Programa --" style="width:100%">
                        @*                        @foreach (var item in Model.Programa)
                            {
                            <option value="@item.IdPrograma">@item.Nombre</option>
                            }*@
                    </select>
                </div>

                <div class="form-group mb-2">
                    <select class="form-control select2" name="sedes" id="sedes" multiple="multiple" data-placeholder="-- Seleccionar Sede --" style="width:100%">
                        @*                        @foreach (var item in Model.LIstaSedes)
                            {
                            <option value="@item.IdSede">@item.Nombre</option>
                            }*@
                    </select>
                </div>

                <div class="form-group mb-2">
                    <select class="form-control select2" name="carreras" id="carreras" multiple="multiple" data-placeholder="-- Seleccionar Carrera --" style="width:100%">
                        @*                        @foreach (var item in Model.Carreras)
                            {
                            <option value="@item.IdCarrera">@item.Nombre</option>
                            }*@
                    </select>
                </div>
                <hr align="left" noshade="noshade" size="2" width="100%" />
                <div class="form-group mb-2">
                    <select class="form-control select2" name="sexos" id="sexos" multiple="multiple" data-placeholder="-- Sexo --" style="width:100%">
                        @foreach (var item in Model.sexos)
                        {
                            <option value="@item.IdSexo">@item.Nombre</option>
                        }
                    </select>
                </div>
                <div class="form-group mb-2">
                    <select class="form-control select2" name="tipomatricula" id="tipomatricula" multiple="multiple" data-placeholder="-- Tipo Matrícula --" style="width:100%">
                        @foreach (var item in Model.tipomatricula)
                        {
                            <option value="@item.IdTipoMatricula">@item.Nombre</option>
                        }
                    </select>
                </div>
                <div class="form-group mb-2">
                    <select class="form-control select2" name="departament" id="departament" multiple="multiple" data-placeholder="-- Departamento de residencia --" style="width:100%">
                        @foreach (var item in Model.departamentos)
                        {
                            <option value="@item.IdDepartamento">@item.Nombre</option>
                        }
                    </select>
                </div>
                <div class="form-group mb-2">
                    <select class="form-control select2" name="refiere" id="refiere" multiple="multiple" data-placeholder="-- Institución que refiere --" style="width:100%">
                        @foreach (var item in Model.dropRefiereCM)
                        {
                            <option value="@item.IdRefiere">@item.Nombre</option>
                        }
                    </select>
                </div>
                <div class="form-group mb-2">
                    <select class="form-control select2" name="year1" id="year1" multiple="multiple" data-placeholder="-- Año de reporte --" style="width:100%">
                        @foreach (var item in Model.catAños)
                        {
                            <option value="@item.Nombre">@item.Nombre</option>
                        }
                    </select>
                </div>
                <div class="form-group mb-2">
                    <select class="form-control select2" name="mes" id="mes" multiple="multiple" data-placeholder="-- Mes de reporte --" style="width:100%">
                        @foreach (var item in Model.mes)
                        {
                            <option value="@item.Nombre">@item.Nombre</option>
                        }
                    </select>
                </div>
                <div class="form-group mb-2">
                    <select class="form-control select2" name="cohorte1" id="cohorte1" multiple="multiple" data-placeholder="-- Cohorte de estudios --" style="width:100%">
                        @foreach (var item in Model.cohorte)
                        {
                            <option value="@item.IdCohorte">@item.Nombre</option>
                        }
                    </select>
                </div>
                <div class="form-group mb-2">
                    <select class="form-control select2" name="aEstudio" id="aEstudio" multiple="multiple" data-placeholder="-- Año de Educación --" style="width:100%">
                        @foreach (var item in Model.catAños)
                        {
                            <option value="@item.Nombre">@item.Nombre</option>
                        }
                    </select>
                </div>
                <div class="form-group mb-2">
                    <select class="form-control select2" name="ePersona" asp-items="ViewBag.EstadoPersona" multiple="multiple" data-placeholder="-- Estado Educación --" style="width:100%"></select>
                </div>
                <button id="btnFilters" type="submit" class="btn btn-primary mb-2">Buscar</button>
            </form>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-header">
        <h3 class="card-title">Educación</h3>
    </div>
    <!-- /.card-header -->
    <div class="card-body">
        <table id="example1" class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Año de reporte</th>
                    <th>Mes de reporte</th>
                    <th>Nombre</th>
                    <th>Socio</th>
                    <th>Sede</th>
                    <th>Programa</th>
                    <th>Año de estudio</th>
                    <th>Cohorte</th>
                    <th>Carrera</th>
                    <th>Tipo de matricula</th>
                    <th>Sexo</th>
                    <th>Fecha Reasignacion</th>
                    <th>Estado</th>
                    <th>Fecha de deserción</th>
                    <th>Motivo de deserción</th>
                    <th>Dias de Asistencia Establecidos</th>
                    <th>Dias de Asistencia Efectivos</th>
                    <th>Porcentaje de asistencia</th>
                    <th>Motivo de Inasistencia</th>
                    <th>Modulos Inscritos</th>
                    <th>Modulos Aprobados</th>
                    <th>Modulos Reprobados</th>
                    <th>Causa Reprobacion</th>
                </tr>
            </thead>
            <tbody>
            </tbody>
        </table>
    </div>
    <!-- /.card-body -->
</div>

@section scripts{
<script>
        $(function () {
            // Lista de selects y objeto para almacenar los valores seleccionados
            var selectList = ['socios', 'dropzona', 'idprograma', 'sedes', 'carreras'];
            var selectedValues = {
                socios: [],
                dropzona: [],
                idprograma: [],
                sedes: [],
                carreras: [],
                sexos: [],
                tipomatricula: [],
                departament: [],
                refiere: [],
                year1: [],
                mes: [],
                aEstudio: [],
                cohorte: [],
                proyecto: [],
                estado: [],
                retornado: []
            };

            var updateSelectedValues = function (id, value) {
                selectedValues[id] = value;
            };

            const clearSubsequentSelects = function (currentId) {
                var index = selectList.indexOf(currentId);
                if (index !== -1) {
                    for (var i = index + 1; i < selectList.length; i++) {
                        var selectId = `#${selectList[i]}`;
                        $(selectId).val(null).trigger('change');
                        $(selectId).empty().append(`<option value> -- Seleccionar ${selectList[i]} -- </option>`);
                    }
                }
            };

            const populateDropdown = function (url, id, target) {
                $(id).on('change', function () {
                    var selectedIds = $(id).val();
                    updateSelectedValues(id.substring(1), selectedIds);

                    // Clear subsequent selects
                    clearSubsequentSelects(id.substring(1));

                    var data = {};

                    // Modify the data payload based on the selected values
                    if (id === '#socios') {
                        data = { id: selectedIds[0] };
                    } else if (id === '#dropzona') {
                        var selectedIds2 = $('#socios').val();
                        data = { id: selectedIds[0], ids: selectedIds2[0] };
                    } else if (id === '#idprograma') {
                        var selectedIds2 = $('#socios').val();
                        var selectedIds3 = $('#dropzona').val();
                        data = { id: selectedIds3[0], ids: selectedIds2[0], idp: selectedIds[0] };
                    } else if (id === '#sedes') {
                        var selectedIds2 = $('#socios').val();
                        var selectedIdP = $('#idprograma').val();
                        data = { id: selectedIds[0], ids: selectedIds2[0], idp: selectedIdP[0] };
                    }

                    $.get(url, data, function (data) {
                        $(target).empty();
                        $(target).append(`<option value> -- Seleccionar ${target.substring(1)} -- </option>`);
                        $.each(data, function (index, row) {
                            $(target).append(`<option value="${row.id}">${row.nombre}</option>`);
                        });
                    });
                }).trigger('change');
            };

            populateDropdown('/Mantenimientos/GetZona', '#socios', '#dropzona');
            populateDropdown('/Mantenimientos/GetProgramas', '#dropzona', '#idprograma');
            populateDropdown('/Mantenimientos/GetSedes', '#idprograma', '#sedes');
            populateDropdown('/Mantenimientos/GetCarreras', '#sedes', '#carreras');

            $("#example1").DataTable({
                "language": {
                    "sProcessing": "Procesando...",
                    "sLengthMenu": "Mostrar _MENU_ registros",
                    "sZeroRecords": "No se encontraron resultados",
                    "sEmptyTable": "Ningún dato disponible en esta tabla",
                    "sInfo": "Mostrando registros del _START_ al _END_ de un total de _TOTAL_ registros",
                    "sInfoEmpty": "Mostrando registros del 0 al 0 de un total de 0 registros",
                    "sInfoFiltered": "(filtrado de un total de _MAX_ registros)",
                    "sInfoPostFix": "",
                    "sSearch": "Buscar:",
                    "sUrl": "",
                    "sInfoThousands": ",",
                    "sLoadingRecords": "Cargando...",
                    "oPaginate": {
                        "sFirst": "Primero",
                        "sLast": "Último",
                        "sNext": "Siguiente",
                        "sPrevious": "Anterior"
                    },
                    "oAria": {
                        "sSortAscending": ": Activar para ordenar la columna de manera ascendente",
                        "sSortDescending": ": Activar para ordenar la columna de manera descendente"
                    }
                },
                "responsive": true, "lengthChange": false, "autoWidth": false,
                "ajax": {
                    "url": "/Mantenimientos/Educacion",
                    "type": "POST",
                    "dataType": "json",
                    "data": function (d) {
                        d.socios = selectedValues.socios;
                        d.dropzona = selectedValues.dropzona;
                        d.idprograma = selectedValues.idprograma;
                        d.sedes = selectedValues.sedes;
                        d.carreras = selectedValues.carreras;
                        d.sexos = selectedValues.sexos;
                        d.tipomatricula = selectedValues.tipomatricula;
                        d.departament = selectedValues.departament;
                        d.refiere = selectedValues.refiere;
                        d.year1 = selectedValues.year1;
                        d.mes = selectedValues.mes;
                        d.aEstudio = selectedValues.aEstudio;
                        d.cohorte = selectedValues.cohorte;
                        d.proyecto = selectedValues.proyecto;
                        d.estado = selectedValues.estado;
                        d.retornado = selectedValues.retornado;
                        return d;
                    }
                },
                "columns": [
                    { "data": "pIdOim" },
                    { "data": "rAño" },
                    { "data": "rMes" },
                    { "data": "nombre" },
                    { "data": "socio" },
                    { "data": "sede" },
                    { "data": "programaNombre" },
                    { "data": "year" },
                    { "data": "cohorteNombre" },
                    { "data": "carrera" },
                    { "data": "tipoMatricula" },
                    { "data": "sexoNombre" },
                    { "data": "dFechaReasg" },
                    { "data": "dEstado" },
                    { "data": "dFechades" },
                    { "data": "motivodesercion" },
                    { "data": "iDiasAsistenciaEstablecidos" },
                    { "data": "iDiasAsistenciaEfectivos" },
                    { "data": "pocentajeasistencia" },
                    { "data": "iMotivoInasistencia" },
                    { "data": "iModulosInscritos" },
                    { "data": "iModulosAprobados" },
                    { "data": "iModulosReprobados" },
                    { "data": "iCausaReprobacion" }
                ],
                "buttons": [
                    {
                        extend: 'copy',
                        text: 'Copiar'
                    },
                    {
                        extend: 'csv',
                        text: 'CSV'
                    },
                    {
                        extend: 'excel',
                        text: 'Excel'
                    },
                    {
                        extend: 'pdf',
                        text: 'PDF'
                    },
                    {
                        extend: 'print',
                        text: 'Imprimir'
                    },
                    {
                        extend: 'colvis',
                        text: 'Visibilidad de Columnas'
                    }
                ]
            }).buttons().container().appendTo('#example1_wrapper .col-md-6:eq(0)');

            // Evento para filtrar los datos
            $('#btnFilters').on('click', function (e) {
                e.preventDefault();
                $("#example1").DataTable().ajax.reload();
            });

            $('#carreras').on('change', function () {
                var selectedData = $(this).select2('data');
                var selectedIds = selectedData.map(function (option) {
                    return option.id;
                });
                selectedValues.carreras = selectedIds;
            });

             //Descomenta las siguientes líneas si necesitas usar los select2 para otros campos
             $('#sexos').on('change', function () {
                 var selectedData = $(this).select2('data');
                 var selectedIds = selectedData.map(function (option) {
                     return option.id;
                 });
                 selectedValues.sexos = selectedIds;
             });

             $('#tipomatricula').on('change', function () {
                 var selectedData = $(this).select2('data');
                 var selectedIds = selectedData.map(function (option) {
                     return option.id;
                 });
                 selectedValues.tipomatricula = selectedIds;
             });


             $('#departament').on('change', function () {
                 var selectedData = $(this).select2('data');
                 var selectedIds = selectedData.map(function (option) {
                     return option.id;
                 });
                 selectedValues.departament = selectedIds;
             });
             $('#refiere').on('change', function () {
                 var selectedData = $(this).select2('data');
                 var selectedIds = selectedData.map(function (option) {
                     return option.id;
                 });
                 selectedValues.refiere = selectedIds;
             });
             $('#year1').on('change', function () {
                 var selectedData = $(this).select2('data');
                 var selectedIds = selectedData.map(function (option) {
                     return option.id;
                 });
                 selectedValues.year1 = selectedIds;
             });
             $('#aEstudio').on('change', function () {
                 var selectedData = $(this).select2('data');
                 var selectedIds = selectedData.map(function (option) {
                     return option.id;
                 });
                 selectedValues.aEstudio = selectedIds;
             });
             $('#cohorte').on('change', function () {
                 var selectedData = $(this).select2('data');
                 var selectedIds = selectedData.map(function (option) {
                     return option.id;
                 });
                 selectedValues.cohorte = selectedIds;
             });
             $('#proyecto').on('change', function () {
                 var selectedData = $(this).select2('data');
                 var selectedIds = selectedData.map(function (option) {
                     return option.id;
                 });
                 selectedValues.proyecto = selectedIds;
             });
             $('#estado').on('change', function () {
                 var selectedData = $(this).select2('data');
                 var selectedIds = selectedData.map(function (option) {
                     return option.id;
                 });
                 selectedValues.estado = selectedIds;
             });
             $('#retornado').on('change', function () {
                 var selectedData = $(this).select2('data');
                 var selectedIds = selectedData.map(function (option) {
                     return option.id;
                 });
                 selectedValues.retornado = selectedIds;
             });

        $('.select2').select2();
    });
</script>

}